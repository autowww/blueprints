# lenses — machine-local state (forge-lenses)
.lenses-local/

# local forge session logs (not source)
forge-logs/

# Firebase CLI debug spill (never commit)
docs/firebase-debug.log
firebase-debug.log
