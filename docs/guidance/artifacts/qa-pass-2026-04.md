# Final depth QA (Prompt 9)

Checklist source: [08-review-checklist.md](../08-review-checklist.md). Maintainer-only; not published on the handbook.

**Scope of this pass:** Re-validate public handbook depth after **Prompt 7** (topic splits + onboarding nav order + Lenses handbook splits), **Prompt 6** (Kitchen Sink `blueprint-diagram` tiles on key pages), and **Prompt 8** (worked example / scenario tables). This document supersedes the pre-split snapshot in the earlier revision of this file.

## Verdict summary

| Track | Result | Notes |
|-------|--------|--------|
| Blueprints SDLC onboarding | **Pass** (ongoing polish OK) | Parents are choosers with child pages; [`_ONBOARDING_NAV_ORDER`](../../../../blueprints-website/generator/build-handbook.py) defines prev/next journey; hub caps still apply only to catalog hubs per validator |
| forge-lenses handbook-public | **Pass** (ongoing polish OK) | Child pages use `_NN-` stems so `simple_build` sort keeps parent before children; manifest globs cover splits; KS diagram tiles on overview/setup/Wizard 201 |
| Generator / policy | **Pass** | `validate_public_handbook_surface.py` clean; no Mermaid in handbook Markdown (workspace policy); operator-leak lints unchanged |

## Checklist mapping ([08](../08-review-checklist.md))

| Section | Assessment |
|---------|------------|
| **Coverage** | Onboarding and Lenses splits state what/when/prereqs; chooser parents link children; scenario tables add concreteness where topics were abstract |
| **Structure** | Skimmable headings; tables and short sections; child pages avoid prose walls |
| **Visuals** | Primary `blueprint-diagram` tiles where flow, comparison, scale, or modes matter; tables remain where precision matters |
| **Boundary** | Public-facing; maintainer detail remains on GitHub where noted; no unnecessary build-internals in public Lenses bodies |
| **Navigation** | Onboarding order explicit in generator; Lenses hub/sidebars list split pages; parent pages link to children |
| **Quality bar** | Readable in one sitting for chooser pages; practitioners can jump to a child for execution detail |

## Spot-check notes

| Question | Outcome |
|----------|---------|
| Child pages + prev/next | Onboarding journey ordered via `_ONBOARDING_NAV_ORDER`; pages not in tuple append sorted (expected for any future stray onboarding pages) |
| KS diagram tiles | Render as `forge-diagram` + `assets/svg/template-*.svg`; catalog keys used (`decision`, `linear`, `timeline`, `tree`, `quadrant`, `checklist`, etc.) |
| Examples | ICP paths A/B/C, first hour, Forge Studio, submodule recovery, single-team rollout, Wizard 101 + Wizard 201 modes covered with scenario structure |

## Remaining polish (non-blocking)

- Deeper **Studio 201 / 301** visuals and examples if those pages grow further (see [06-coverage-plan-lenses-studio-wizard.md](../06-coverage-plan-lenses-studio-wizard.md)).
- Optional **custom SVG instances** under `blueprints/sdlc/docs/assets/` where a template tile is not specific enough (follow [03-visual-language-and-ks-usage.md](../03-visual-language-and-ks-usage.md)).

## Builds run (this pass)

- `python3 generator/validate_public_handbook_surface.py` — **OK** (`blueprints-website` repo root)
- Prior full handbook generation: `python3 generator/build-handbook.py --all --incremental` and `python3 generator/inject-portal-nav.py` — **OK**
