# Claude Code subagent templates

Subagent definitions copied to `.claude/agents/` by `init-ide-workspace.sh`. Subagents run in a separate context with scoped tools.

| Template | Subagent | Purpose |
|----------|----------|---------|
| `spec-reviewer.template.md` | `spec-reviewer` | Review code changes against spec acceptance criteria |
